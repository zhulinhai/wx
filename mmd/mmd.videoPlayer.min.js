this.createjs = this.createjs || {}, function() {
	"use strict";

	function t(t, i, e, n, s, r) {
		this.setValues(t, i, e, n, s, r)
	}
	var i = t.prototype;
	t.DEG_TO_RAD = Math.PI / 180, t.identity = null, i.setValues = function(t, i, e, n, s, r) {
		return this.a = null == t ? 1 : t, this.b = i || 0, this.c = e || 0, this.d = null == n ? 1 : n, this.tx = s || 0, this.ty = r || 0, this
	}, i.append = function(t, i, e, n, s, r) {
		var a = this.a,
			o = this.b,
			h = this.c,
			d = this.d;
		return 1 == t && 0 == i && 0 == e && 1 == n || (this.a = a * t + h * i, this.b = o * t + d * i, this.c = a * e + h * n, this.d = o * e + d * n), this.tx = a * s + h * r + this.tx, this.ty = o * s + d * r + this.ty, this
	}, i.prepend = function(t, i, e, n, s, r) {
		var a = this.a,
			o = this.c,
			h = this.tx;
		return this.a = t * a + e * this.b, this.b = i * a + n * this.b, this.c = t * o + e * this.d, this.d = i * o + n * this.d, this.tx = t * h + e * this.ty + s, this.ty = i * h + n * this.ty + r, this
	}, i.appendMatrix = function(t) {
		return this.append(t.a, t.b, t.c, t.d, t.tx, t.ty)
	}, i.prependMatrix = function(t) {
		return this.prepend(t.a, t.b, t.c, t.d, t.tx, t.ty)
	}, i.appendTransform = function(i, e, n, s, r, a, o, h, d) {
		if (r % 360) var u = r * t.DEG_TO_RAD,
			c = Math.cos(u),
			l = Math.sin(u);
		else c = 1, l = 0;
		return a || o ? (a *= t.DEG_TO_RAD, o *= t.DEG_TO_RAD, this.append(Math.cos(o), Math.sin(o), -Math.sin(a), Math.cos(a), i, e), this.append(c * n, l * n, -l * s, c * s, 0, 0)) : this.append(c * n, l * n, -l * s, c * s, i, e), (h || d) && (this.tx -= h * this.a + d * this.c, this.ty -= h * this.b + d * this.d), this
	}, i.prependTransform = function(i, e, n, s, r, a, o, h, d) {
		if (r % 360) var u = r * t.DEG_TO_RAD,
			c = Math.cos(u),
			l = Math.sin(u);
		else c = 1, l = 0;
		return (h || d) && (this.tx -= h, this.ty -= d), a || o ? (a *= t.DEG_TO_RAD, o *= t.DEG_TO_RAD, this.prepend(c * n, l * n, -l * s, c * s, 0, 0), this.prepend(Math.cos(o), Math.sin(o), -Math.sin(a), Math.cos(a), i, e)) : this.prepend(c * n, l * n, -l * s, c * s, i, e), this
	}, i.rotate = function(i) {
		i *= t.DEG_TO_RAD;
		var e = Math.cos(i),
			n = Math.sin(i),
			s = this.a,
			r = this.b;
		return this.a = s * e + this.c * n, this.b = r * e + this.d * n, this.c = -s * n + this.c * e, this.d = -r * n + this.d * e, this
	}, i.skew = function(i, e) {
		return i *= t.DEG_TO_RAD, e *= t.DEG_TO_RAD, this.append(Math.cos(e), Math.sin(e), -Math.sin(i), Math.cos(i), 0, 0), this
	}, i.scale = function(t, i) {
		return this.a *= t, this.b *= t, this.c *= i, this.d *= i, this
	}, i.translate = function(t, i) {
		return this.tx += this.a * t + this.c * i, this.ty += this.b * t + this.d * i, this
	}, i.identity = function() {
		return this.a = this.d = 1, this.b = this.c = this.tx = this.ty = 0, this
	}, i.invert = function() {
		var t = this.a,
			i = this.b,
			e = this.c,
			n = this.d,
			s = this.tx,
			r = t * n - i * e;
		return this.a = n / r, this.b = -i / r, this.c = -e / r, this.d = t / r, this.tx = (e * this.ty - n * s) / r, this.ty = -(t * this.ty - i * s) / r, this
	}, i.isIdentity = function() {
		return 0 === this.tx && 0 === this.ty && 1 === this.a && 0 === this.b && 0 === this.c && 1 === this.d
	}, i.equals = function(t) {
		return this.tx === t.tx && this.ty === t.ty && this.a === t.a && this.b === t.b && this.c === t.c && this.d === t.d
	}, i.transformPoint = function(t, i, e) {
		return e = e || {}, e.x = t * this.a + i * this.c + this.tx, e.y = t * this.b + i * this.d + this.ty, e
	}, i.decompose = function(i) {
		null == i && (i = {}), i.x = this.tx, i.y = this.ty, i.scaleX = Math.sqrt(this.a * this.a + this.b * this.b), i.scaleY = Math.sqrt(this.c * this.c + this.d * this.d);
		var e = Math.atan2(-this.c, this.d),
			n = Math.atan2(this.b, this.a),
			s = Math.abs(1 - e / n);
		return s < 1e-5 ? (i.rotation = n / t.DEG_TO_RAD, this.a < 0 && this.d >= 0 && (i.rotation += i.rotation <= 0 ? 180 : -180), i.skewX = i.skewY = 0) : (i.skewX = e / t.DEG_TO_RAD, i.skewY = n / t.DEG_TO_RAD), i
	}, i.copy = function(t) {
		return this.setValues(t.a, t.b, t.c, t.d, t.tx, t.ty)
	}, i.clone = function() {
		return new t(this.a, this.b, this.c, this.d, this.tx, this.ty)
	}, i.toString = function() {
		return "[Matrix2D (a=" + this.a + " b=" + this.b + " c=" + this.c + " d=" + this.d + " tx=" + this.tx + " ty=" + this.ty + ")]"
	}, t.identity = new t, createjs.Matrix2D = t
}();
var makeVideoPlayableInline = function() {
		"use strict";

		function t(t) {
			function i(s) {
				e = requestAnimationFrame(i), t(s - (n || s)), n = s
			}
			var e = void 0,
				n = void 0;
			this.start = function() {
				e || i(0)
			}, this.stop = function() {
				cancelAnimationFrame(e), e = null, n = 0
			}
		}
		function i(t, i, e, n) {
			function s(i) {
				Boolean(t[e]) === Boolean(n) && i.stopImmediatePropagation(), delete t[e]
			}
			return t.addEventListener(i, s, !1), s
		}
		function e(t, i, e, n) {
			function s() {
				return e[i]
			}
			function r(t) {
				e[i] = t
			}
			n && r(t[i]), Object.defineProperty(t, i, {
				get: s,
				set: r
			})
		}
		function n(t) {
			var i = new Audio,
				e = t.currentSrc || t.src;
			return i.src = e, i.crossOrigin = t.crossOrigin, i
		}
		function s(t, i) {
			(m.tue || 0) + 200 < Date.now() && (t[v] = !0, m.tue = Date.now()), t.currentTime = i, m[++m.i % 3] = 100 * i | 0
		}
		function r(t) {
			return t.driver.currentTime >= t.video.duration
		}
		function a(t) {
			var i = this;
			i.video.readyState >= i.video.HAVE_FUTURE_DATA && (i.hasAudio || (i.driver.currentTime = i.video.currentTime + t * i.video.playbackRate / 1e3, i.video.loop && r(i) && (i.driver.currentTime = 0)), s(i.video, i.driver.currentTime)), i.video.ended && i.video.pause(!0)
		}
		function o() {
			var t = this,
				i = t[p];
			return t.webkitDisplayingFullscreen ? void t[y]() : void(t.paused && (i.paused = !1, t.buffered.length || t.load(), i.driver.play(), i.updater.start(), t.dispatchEvent(new Event("play")), t.dispatchEvent(new Event("playing"))))
		}
		function h(t) {
			var i = this,
				e = i[p];
			e.driver.pause(), e.updater.stop(), i.webkitDisplayingFullscreen && i[b](), e.paused && !t || (e.paused = !0, i.dispatchEvent(new Event("pause")), i.ended && (i[v] = !0, i.dispatchEvent(new Event("ended"))))
		}
		function d(i, e) {
			var o = i[p] = {};
			o.paused = !0, o.hasAudio = e, o.video = i, o.updater = new t(a.bind(o)), e ? o.driver = n(i) : o.driver = {
				muted: !0,
				paused: !0,
				pause: function() {
					o.driver.paused = !0
				},
				play: function() {
					o.driver.paused = !1, r(o) && s(i, 0)
				},
				get ended() {
					return r(o)
				}
			}, i.addEventListener("emptied", function() {
				o.driver.src && o.driver.src !== i.currentSrc && (s(i, 0), i.pause(), o.driver.src = i.currentSrc)
			}, !1), i.addEventListener("webkitbeginfullscreen", function() {
				i.paused ? e && !o.driver.buffered.length && o.driver.load() : (i.pause(), i[y]())
			}), e && (i.addEventListener("webkitendfullscreen", function() {
				o.driver.currentTime = i.currentTime
			}), i.addEventListener("seeking", function() {
				m.indexOf(100 * i.currentTime | 0) < 0 && (o.driver.currentTime = i.currentTime)
			}))
		}
		function u(t) {
			var n = t[p];
			t[y] = t.play, t[b] = t.pause, t.play = o, t.pause = h, e(t, "paused", n.driver), e(t, "muted", n.driver, !0), e(t, "playbackRate", n.driver, !0), e(t, "ended", n.driver), e(t, "loop", n.driver, !0), i(t, "seeking"), i(t, "seeked"), i(t, "timeupdate", v, !1), i(t, "ended", v, !1)
		}
		function c(t) {
			var i = arguments.length <= 1 || void 0 === arguments[1] || arguments[1],
				e = arguments.length <= 2 || void 0 === arguments[2] || arguments[2];
			e && !f || t[p] || (d(t, i), u(t), t.classList.add("IIV"), !i && t.autoplay && t.play())
		}
		var l = "undefined" == typeof Symbol ?
		function(t) {
			return "@" + (t || "@") + Math.random()
		} : Symbol, f = /iPhone|iPod/i.test(navigator.userAgent), p = l(), v = l(), y = l("nativeplay"), b = l("nativepause"), m = [];
		return m.i = 0, c
	}();
!
function(t) {
	function i(t, i, e, n, s, r, a) {
		function o() {
			"none" == m.style.display && (m.style.display = "block")
		}
		function h(t) {
			t.style.position = "absolute", t.style.transformOrigin = t.style.WebkitTransformOrigin = t.style.msTransformOrigin = t.style.MozTransformOrigin = t.style.OTransformOrigin = "0% 0%", T.push(t)
		}
		function d() {
			setTimeout(function() {
				document.body.style.zoom = 1.01, setTimeout(function() {
					document.body.style.zoom = 1, u(window.innerWidth, window.innerHeight, M, g)
				}, 50)
			}, 50)
		}
		function u(t, i, e, n) {
			console.log("window.orientation:" + window.orientation);
			var s, r, a, o = new createjs.Matrix2D;
			t < i ? e ? (s = t, r = i, a = 0) : (r = t, s = i, a = 90) : e ? (r = t, s = i, a = 90 == window.orientation ? -90 : 90) : (s = t, r = i, a = 0);
			var h;
			e ? ("width" == n ? h = s / w : "height" == n ? h = r / x : "auto" == n && (h = s / r > w / x ? s / w : r / x), h = Math.ceil(100 * h) / 100, t > i ? 90 == window.orientation ? (o.tx = (t - x * h) / 2, o.ty = w * h + (i - w * h) / 2) : (o.tx = x * h + (t - x * h) / 2, o.ty = (i - w * h) / 2) : (o.tx = (s - w * h) / 2, o.ty = (r - x * h) / 2), o.scale(h, h), o.rotate(a)) : ("width" == n ? h = s / w : "height" == n ? h = r / x : "auto" == n && (h = s / r > w / x ? s / w : r / x), h = Math.ceil(100 * h) / 100, t > i ? (o.tx = (s - w * h) / 2, o.ty = (r - x * h) / 2) : (o.tx = x * h + (t - x * h) / 2, o.ty = (i - w * h) / 2), o.scale(h, h), o.rotate(a));
			for (var d = 0; d < T.length; d++) {
				var u = T[d],
					c = u.style,
					l = "matrix(" + (o.a * D | 0) / D + "," + (o.b * D | 0) / D + "," + (o.c * D | 0) / D + "," + (o.d * D | 0) / D + "," + (o.tx + .5 | 0);
				c.transform = c.WebkitTransform = c.OTransform = c.msTransform = l + "," + (o.ty + .5 | 0) + ")", c.MozTransform = l + "px," + (o.ty + .5 | 0) + "px)"
			}
			// var videos = document.getElementsByTagName('video');
			// var bodyW = document.body.clientWidth;
			// var bodyH = document.body.clientHeight;
			// var STANDARD_WIDTH = 750;
			// var STANDARD_HEIGHT = 1206;
		 // 	var height = bodyW/(750/1206);
			// for(var d =0; d < videos.length; d++){
			// 	var video = videos[d];
			// 	if(height >= bodyH){
		 //            video.style.width = bodyW + 'px';
		 //            video.style.top = (bodyH-height) + 'px';
		 //        }else {
		 //            video.style.height = bodyH + 'px';
		 //            video.style.left = (bodyW - scale * bodyH)/2 + 'px';
		 //        }
			// }
		}
		function c() {
			var t = navigator.userAgent,
				i = !! t.match(/\(i[^;]+;( U;)? CPU.+Mac OS X/);
			return i
		}
		function l() {
			var t = navigator.userAgent;
			return "MicroMessenger" == t.match(/MicroMessenger/i)
		}
		function f() {
			var t = navigator.userAgent;
			return "QQ/" == t.match(/QQ\//i)
		}
		function p() {
			var t = window.navigator.userAgent.toLowerCase(),
				i = "";
			if (t.indexOf("iphone") >= 0) return i = t.indexOf("os 10") >= 0 ? "ios10s" : t.indexOf("os 9") >= 0 ? "ios9s" : t.indexOf("os 8") >= 0 ? "ios8" : "iosOther"
		}
		function v(t) {
			l() && c() ? "object" == typeof WeixinJSBridge ? WeixinJSBridge.invoke("WeixinJSBridgeReady", {}, function(i) {
				t()
			}) : document.addEventListener("WeixinJSBridgeReady", function(i) {
				t()
			}, !1) : t()
		}
		function y() {
			var t = !1;
			return c() && (l() || f()) && (t = "ios8" != p() || "iosOther" != p()), t
		}
		var b, m, w, x, g, M, T = [],
			D = 1e4;
		console.log("VideoPlayer init"), b = t, m = i, m.setAttribute("x-webkit-airplay", "true"), m.setAttribute("airplay", "allow"), m.setAttribute("webkit-playsinline", "true"), m.setAttribute("playsinline", "true"), m.addEventListener("touchstart", function(t) {
			t.preventDefault()
		}), m.addEventListener("canplay", o), m.addEventListener("canplaythrough", o), m.addEventListener("timeupdate", o), m.src = e, w = n, x = s, g = void 0 == r ? "auto" : r, M = void 0 == a || a, c() && ("ios8" == p() || "iosOther" == p() ? makeVideoPlayableInline(m) : f() || l() || makeVideoPlayableInline(m)), window.orientation = window.orientation || 0, h(b), u(document.body.clientWidth, document.body.clientHeight, M, g), window.addEventListener("orientationchange", function(t) {
			d()
		}), window.addEventListener("resize", function(t) {
			d()
		}), this.isVideoCanAutoPlay = y(), this.play = function() {
			v(function() {
				m.play()
			})
		}
	}
	t.VideoPlayer = i
}(this.MMD = this.MMD || {});